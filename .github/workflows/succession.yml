name: retired-workflow-placeholder

# This filename is retained only to neutralize the pre-0.13 workflow when a
# release ZIP is applied over an older checkout. The maintained workflow is
# ci.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. Use the agent-successor-benchmark workflow."
